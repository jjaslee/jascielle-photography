# Copy to .env for local dev (do not commit .env if it contains secrets).
# In Vercel: Project → Settings → Environment Variables → same name.
VITE_FORMSPREE_ENDPOINT=https://formspree.io/f/yourFormId
